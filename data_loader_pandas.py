import pandas as pd


def csv_to_pd(csv_filepath: str) -> pd.DataFrame:
    """Convert raw CSV file into pandas.DataFrame.
    
    pandas.DataFrame is a more efficient form of array-like object that is
    more memory efficient than raw lists of Deal objects. It also includes
    built-in functions that allow efficient sorting of rows/columns and indexing."""
    pass